{
  "mcpServers": {
    "supabase": {
      "command": "npx",
      "args": [
        "-y",
        "@supabase/mcp-server-supabase@latest",
        "--access-token",
        "$SUPABASE_MCP_TOKEN",
        "$SUPABASE_MCP_EXTRA_ARGS"
      ],
      "notes": "Do NOT commit real tokens. Set SUPABASE_MCP_TOKEN in your Codespace and optionally SUPABASE_MCP_EXTRA_ARGS='--port 34211' before starting. Example: export SUPABASE_MCP_TOKEN=\"<token>\"; export SUPABASE_MCP_EXTRA_ARGS='--port 34211'"
    }
  }
}
