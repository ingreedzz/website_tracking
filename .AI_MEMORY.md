# AI Agent Memory (ephemeral project memory saved in repo)

This file records agent-visible context to help planning and testing.

Important facts (as of 2025-11-13):

- Connections and integrations:
  - Vercel: frontend deploys configured; Vercel proxy rewrites to Render for `/api/*`.
  - Render: backend is deployed to Render (health endpoint available at `/api/health`).
  - Supabase: Postgres + Storage used for database and file storage. Repository secrets `SUPABASE_URL` and `SUPABASE_KEY` have been added in GitHub.
  - GitHub MCP: CI/workflows and delegated agents configured (CodeRabbit / MCP integrations present).

- DB schema notes:
  - `backend/database/schema.sql` includes `users.role text DEFAULT 'customer'`.
  - Manual migration scripts exist to remove/restore the `role` column:
    - `backend/database/remove_role_migration.sql`
    - `backend/database/restore_role_migration.sql`
    - `backend/database/README_REMOVE_ROLE.md` (instructions/warnings)

- Current code state:
  - Backend tolerates missing `role` column: registration/login now include `is_admin` in tokens and retry user creation without `role` if DB rejects the column.
  - Admin checks use `is_admin` where possible; `role` is derived for compatibility.

Agent policy for role removal:
- I WILL NOT run destructive DB migrations without explicit owner confirmation and valid Supabase credentials or an owner-triggered CI job.
- Steps required before removal: create DB backup (pg_dump or Supabase snapshot), verify backup integrity, run `remove_role_migration.sql` in Supabase SQL editor, run smoke tests, if issues run `restore_role_migration.sql`.

If you want me to perform the removal now, reply with explicit confirmation and provide one of the following:
  - Temporary Supabase service_role key (set as environment variables here or in a secure GitHub Actions workflow), OR
  - Permission to create a GitHub Actions workflow_dispatch job that the repo owner can run (preferred for security).

This file is for agent planning and reference; update or delete as needed.
